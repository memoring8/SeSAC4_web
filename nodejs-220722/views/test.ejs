<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title> ejs </title>
</head>
<body>
    <!-- 
        for ( var i = 0; i < 5; i++) {
        if(i==3){
            // h3 태그
        } else {
            // h1 태그
        }
        }
    -->

    <% for (var i =0; i <5; i++) { %>
        <% if (i==3) { %>
            <h3> hi </h3>  
        <% } else { %>
            <h1> hi </h1>
        <% } %>
    <% } %>

    <% var a = 'hello'; %>
    <p><%=a %></p>

    <%- include('test2.ejs')%>
</body>
</html>